# AI PhotoSearch (Portable)

Это приложение позволяет искать изображения по текстовому описанию и/или названию файла с помощью нейросети CLIP.

Вам не нужно устанавливать Python или библиотеки — отдельная portable-среда Python предоставляется отдельно.

---

## 🚀 Быстрый старт

1. **Склонируйте или скачайте этот репозиторий** на свой компьютер.
2. **Скачайте архив portable Python [`system.zip`](https://drive.google.com/open?id=1-_KxftuNN8I320GGNvG2oZjBJodxAERH&usp=drive_fs с Google Drive и распакуйте его в корень проекта, чтобы появилась папка `system/` рядом с `app.py` и `run_app.bat`.
3. Структура вашей папки должна выглядеть так:
    ```
    AI_PhotoSearch/
    ├── app.py
    ├── run_app.bat
    ├── system/
    │   └── python/
    │        ├── python.exe
    │        ├── ... (DLLs, site-packages, etc.)
    ├── cache/                (создаётся автоматически)
    ├── last_used_paths.pkl   (создаётся автоматически)
    ```
4. **Дважды кликните по `run_app.bat`** для запуска приложения.
    - Приложение откроется в браузере по адресу [http://localhost:8501/](http://localhost:8501/).
    - Если оно не открылось автоматически, скопируйте адрес из терминала и вставьте в браузер вручную.

---

## 🖼️ Как пользоваться

- **Folder path:** Введите или вставьте путь к папке с изображениями (совет: в Проводнике Windows кликните по папке правой кнопкой и выберите “Копировать как путь”).
- **Image description:** Введите описание для смыслового поиска (через CLIP).
- **File name:** Поиск по имени или его части.
- **Scale:** Настраивает количество колонок для отображения.
- **Number of results:** Ограничивает количество отображаемых изображений.
- **Show all:** Показывает всю базу изображений.
- **Save:** Сохраняет найденные изображения в новую папку.

---

## 📄 Часто задаваемые вопросы (FAQ)

- **Не нужно устанавливать Python или библиотеки:** Просто скачайте и распакуйте архив `system`, как описано выше.
- **История выбранных папок** хранится в `last_used_paths.pkl` (создаётся автоматически в папке приложения).
- **Кеш признаков изображений** хранится в папке `cache/` (создаётся автоматически).
- **Если приложение не запускается:** Убедитесь, что вы запускаете `run_app.bat` и что в вашей системе разрешён запуск `.bat` файлов.
- **Очистить историю выбранных папок:** Удалите файл `last_used_paths.pkl` (он создастся заново автоматически).
- **Портируемость:** Вы можете скопировать всю папку на другой компьютер и запускать точно так же (только не забудьте также скачать и распаковать архив `system`).

---

## 🔗 Portable Python Download

> **Скачайте portable-среду Python (`system.zip`) с [Google Drive](https://drive.google.com/open?id=1-_KxftuNN8I320GGNvG2oZjBJodxAERH&usp=drive_fs) и распакуйте её в корень проекта.**
>
> Вам не нужно устанавливать Python или зависимости — всё необходимое уже есть в папке `system`.

---

## ✉️ Контакты

Если возникли вопросы, пишите автору скрипта: romanlosev@gmail.com

---

**Приятного использования AI PhotoSearch!**
